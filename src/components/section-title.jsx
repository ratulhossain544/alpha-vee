import React from 'react'

const SectionTitle = ({className, children}) => {
  return (
    <h2 className='text-[2.75rem] leading-[120%]'>
        {children}
    </h2>
  )
}

export default SectionTitle